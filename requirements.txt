# ============================================================
# BYBIT CRYPTO BREAKOUT BOT – runtime dependencies
# Lean set: only what the bot actually imports. (No MT5 / ML / news libs.)
# ============================================================

# ----- CLI + config -----
typer[all]>=0.12.3
rich>=13.7.1
python-dotenv>=1.0.1
pyyaml>=6.0.2
pydantic>=2.7.0

# ----- data / indicators -----
numpy>=1.26.4
pandas>=2.2.2

# ----- Bybit V5 SDK -----
pybit>=5.8.0

# ----- trade journal -----
SQLAlchemy>=2.0.30

# ----- logging + alert HTTP -----
loguru>=0.7.2
httpx>=0.27.0

# ----- tests (dev only) -----
pytest>=8.2.0
